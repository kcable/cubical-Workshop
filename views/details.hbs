<div class="container">

  <main>
    <h1>{{name}}</h1>
    <img class="cube" src="{{imageUrl}}">
    <div class="details">
      <p><span>Description:</span>{{description}}</p>
      <p><span>Difficulty level:</span>{{difficulty}}</p>
      {{#if isLoggedIn}}
      <a class="btn" href="/attach/accessory/{{_id}}">Attach acessory</a>
      <a class="btn" href="/edit/{{_id}}">EDIT</a>
      <a class="btn" href="/delete/{{_id}}">Delete</a>
      {{/if}}

      <a class="btn" href="/">Back</a>


    </div>
    <h2>Accessories</h2>
    {{#each accessories}}
    <div class="accessories">
      <div class="accessory">
        <img src="{{this.imageUrl}}" alt="stickerName">
        <h3>{{this.name}}</h3>
        <p>{{this.description}}</p>
      </div>
    </div>
    {{else}}
    <h3 class="italic">This cube has no accessories yet...</h3>
    {{/each}}
  </main>
</div>